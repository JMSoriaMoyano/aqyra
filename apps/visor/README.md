# apps/visor

El visor IFC (el **cebo**): abre cualquier `.ifc` en el navegador. Esqueleto en Fase 0
(`pnpm` workspace). El desarrollo del visor entra por su propia vía; aquí queda el hueco
del workspace TS para que `just build`/`test` lo contemplen cuando exista.
