# Workspace pnpm — el TS del ecosistema (visor y futuros paquetes front).
# Desde el hilo 3.1 el visor es un paquete REAL (re-home del corte mínimo).
packages:
  - "apps/*"

# pnpm ≥10: los build scripts se aprueban EXPLÍCITAMENTE (esbuild = binario de vite/vitest)
allowBuilds:
  esbuild: true
