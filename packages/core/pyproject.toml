[project]
name = "aqyra-core"
version = "0.0.0"
description = "Núcleo compartido (ifc_utils, grafo_red). Esqueleto — se llena en 0.5 (fin del espejo)."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aqyra_core"]
