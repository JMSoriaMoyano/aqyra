# Reporte de tests (Llave 1) · visor-dashboard-valor (E6.1) · 2026-07-11

> Naturaleza `apps/` (revisión normal). Llave 1 = gate de tests TS del visor. Verificado en un espejo
> fiel del monorepo (`ifcopenshell 0.8.5`, Node/Vitest 2, TypeScript 5) que **reproduce `GOL-PRE-03`
> byte a byte** en las `vistas` antes de emitir la fixture. La firma (Llave 2) es de JM.

## Qué se ejecutó

- `pnpm -r build` (equivalente: `tsc -p tsconfig.json`) — **OK**.
- `pnpm -r typecheck` (`tsc -p tsconfig.check.json`, incluye `src` + `test` + `demo`) — **OK**.
- `pnpm -r test` (`vitest run`) — **21 ficheros, 105 tests, TODO VERDE**.
  - **`test/dashboard.test.ts` (NUEVO): 13 tests verdes.**
  - Golden del visor/`core` **INTACTA**: `skins` (13), `data-state` (7), `estado-dato` (6),
    `coste-5d-e2e` (2), `cumplimiento-6d-e2e` (2), `federado-e2e` (4), `bcf` (4), `idealize` (17),
    `idealize-faces` (8), `ifc-loader` (5), `spatial-tree` (1), `zup-cota`/`zup-ingesta`,
    `ux-behavior`, `cost-heat`, `class-control`, `render-pipeline`, `pre-structural`, `saneamiento`,
    `compliance` — sin cambios ni regresión.

## Aceptación (D-DV-4) — reproduce `GOL-PRE-03`

La fixture `apps/visor/fixtures/proyeccion_valor.json` la EMITE el engine
(`tools/emitir_proyeccion_visor.py`) proyectando la medición ANCLADA de `GOL-PRE-03`
(`entrada/{ARQ,EST}.ifc`, md5 `19a272a5…`/`f1d25192…`) con `criterio/AQ/v2` + `banco/AQ-DEMO/v1`
(coste) y `banco-carbono/generico/v2` (carbono). Verificado: las 3 vistas de coste casan **exactas**
(grupo, `valor_total`, `fuente`) con `GOL-PRE-03/expected.json` (`vistas` idénticas), PEM 7 022,53 →
PEC 10 111,74:

- `coste × espacial` Σ=7 022,53: Nivel 00 566,65 (ifc) · Planta Baja 3 815,28 (ifc) · Nivel 01
  2 502,90 (ifc) · (sin geometría) 137,70 (regla).
- `coste × uniclass` Σ=7 022,53: (sin clasificar) 6 884,83 (—) · (sin geometría) 137,70 (regla).
- `coste × funcional` Σ=7 022,53: Cimentación 22,81 (criterio) · Sys-Portico 543,84 (ifc) · Estructura
  3 754,35 (criterio) · Aulas 1 820,66 (ifc) · Admin 557,77 (ifc) · Carpintería 185,40 (criterio) ·
  (sin geometría) 137,70 (regla).
- `carbono × {espacial, funcional, uniclass}` Σ=7 117,69 kgCO2e (invariante Σ igual en los 3 cortes;
  golden de proyección de carbono = forward).

Invariante Σ (Σ grupos == suma, ±0,01) verificado en las **6 vistas**. Fixture LF, md5
`7fc0a51d29d42d428ea4c0949c2d3346`. **Anclada por VALOR** (reproduce `GOL-PRE-03`), no por md5 — evita
la fragilidad EOL de la lección visor-coste-5D.

## Guardarraíles (intactos)

`proyectar` (E2.2) / `GOL-PRE-03` / el motor `engines/presupuesto` / el esquema C5 / los packs / el
núcleo del visor (viewer, ifc-loader, selección, `data-state`, skins existentes) **NO se tocan**. Sin
cálculo en cliente: `dashboard.ts` LEE el índice (sin three/web-ifc). Registro aditivo (modo `?valor`).
Sin release.
