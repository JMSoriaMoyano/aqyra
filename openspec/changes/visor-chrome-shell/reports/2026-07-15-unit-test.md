# Reporte de unidad · Slice C (dock cableado) — 2026-07-15

Change `visor-chrome-shell` · F2.2 (Plan de Cierre) · cableado CONSERVADOR (Opción 1, ratificada por JM 2026-07-15).

## Qué se cableó (a capacidades YA existentes del visor, D-CH-3)
- **Coste 5D** (botón del viewport): `loader.readCost(modelID)` → si hay `IfcCostSchedule`, colorea por heatmap (`costHeatColor` + `viewer.setCostHeatmap`) y muestra PEM→PEC; si no, **toast honesto** «abre el Maestro 5D». Antes era un toast fijo (no comprobaba el modelo).
- **Cumplimiento 6D** (botón del viewport): `loader.readCompliance(modelID)` → si hay `Pset_Aqyra_Cumplimiento`, colorea por resultado (`colorPorResultado` + `viewer.setCumplimientoColors`); si no, **toast honesto**. Antes, toast fijo.
- **Color por clase** (dock): ahora `viewer.resetColors()` + `aplicarSkin` → sirve además de **volver** desde coste/cumplimiento.
- Resto de herramientas del dock (Describir/Idealizar/Esfuerzos/EC/Memoria/BCF): **stub honesto** sin cambios (IA/motores = epics aparte).

## Test-first (headless, sin wasm)
- `src/dock.ts` · `mapaColorPorElemento(porElemento, exprPorGuid, color)`: helper PURO que replica el mapeo `globalId→expressId→color` del `demo/main.ts` (coste y cumplimiento comparten el patrón).
- `test/dock.test.ts`: mapea solo GUIDs presentes (ignora ausentes) + mapa vacío si no casa. Determinista.

## Llave 1
- `pnpm -r typecheck` + `pnpm -r build` + `pnpm -r test` deben ir VERDE en el gate.
- Golden del visor/`core` **intacta**: no se toca `apps/visor/fixtures`, ni el E2E, ni la cámara D29, ni los golden C4/C3. Solo `apps/aqyra-shell/**` + este reporte.
- Sin Llave 2 (no hay release). Revisión visual del dock coloreando un Maestro 5D/6D queda para JM (DoD por slice).
